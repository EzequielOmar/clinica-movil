import { UidDirective } from './uid.directive';

describe('UidDirective', () => {
  it('should create an instance', () => {
    const directive = new UidDirective();
    expect(directive).toBeTruthy();
  });
});
